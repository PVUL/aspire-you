table:
  name: communities
  schema: public
insert_permissions:
  - role: user
    permission:
      check: {}
      columns:
        - name
        - slug
        - is_public
select_permissions:
  - role: public
    permission:
      columns:
        - id
        - name
        - slug
        - is_public
        - created_at
      filter:
        is_public:
          _eq: true
  - role: user
    permission:
      columns:
        - id
        - name
        - slug
        - is_public
        - created_at
      filter: {}
